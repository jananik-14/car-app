class AuthService {
  /*
   * TODO: Replace this with real role data from backend JWT token after login.
   * Backend should return user role (customer/admin) upon successful authentication,
   * and this should be stored securely (e.g., using flutter_secure_storage) 
   * rather than toggled manually in the UI.
   */
  static String currentUserRole = 'customer';
}
